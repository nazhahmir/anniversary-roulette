# Deal or No Deal Anniversary Game - Environment Variables

# Database Configuration
# Replace with your actual database URL when you set up PostgreSQL
DATABASE_URL=postgresql://neondb_owner:npg_r2O4apgLCnjx@ep-super-star-afogs8jq-pooler.c-2.us-west-2.aws.neon.tech/neondb?sslmode=requireDATABASE_URL=postgresql://test:test@localhost:5432/anniversary_roulettechannel_binding=require

# Server Configuration
PORT=5000
NODE_ENV=development

# Replit Configuration (for development environment)
# This is used by Vite for Replit-specific features
REPL_ID=

# Optional: Session Secret (for Express sessions)
# Generate a random string for production
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Optional: Database Pool Configuration
DB_POOL_MIN=2
DB_POOL_MAX=10

# Optional: CORS Configuration (if needed for production)
CORS_ORIGIN=http://localhost:5000

# Optional: Logging Configuration
LOG_LEVEL=info

# Optional: Game Configuration Overrides
# These can be set to override default game settings
GAME_ENVELOPE_COUNT=6
GAME_MAX_TRIES=3
GAME_TIMER_SECONDS=60

# Optional: Admin Panel Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Optional: Security Headers
SECURE_HEADERS=true

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
